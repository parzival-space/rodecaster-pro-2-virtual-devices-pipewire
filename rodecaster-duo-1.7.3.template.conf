context.modules = [
    # +++++++++++++++++++++++++++++++++++++++++++++++++++++
    # Audio Sources / Input Devices
    # +++++++++++++++++++++++++++++++++++++++++++++++++++++

    # Main Mix
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.main"
            node.description = "RODECaster Duo Main Mix"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX0 AUX1 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Fader 1
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.fader1"
            node.description = "RODECaster Duo Fader 1"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX2 AUX3 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Fader 2
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.fader2"
            node.description = "RODECaster Duo Fader 2"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX4 AUX5 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Fader 3
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.fader3"
            node.description = "RODECaster Duo Fader 3"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX6 AUX7 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Fader 4
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.fader4"
            node.description = "RODECaster Duo Fader 4"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX8 AUX9 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Virtual Fader 1
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.vfader1"
            node.description = "RODECaster Duo Virtual Fader 1"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX10 AUX11 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Virtual Fader 2
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.vfader2"
            node.description = "RODECaster Duo Virtual Fader 2"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX12 AUX13 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },

    # Virtual Fader 3
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.vfader3"
            node.description = "RODECaster Duo Virtual Fader 3"
            audio.position = [ FL FR ]

            capture.props = {
                target.object = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
                audio.position = [ AUX14 AUX15 ]
                stream.dont-remix = true
                node.passive = true
            }

            playback.props = { media.class = "Audio/Source" }
        }
    },


    # +++++++++++++++++++++++++++++++++++++++++++++++++++++
    # Audio Sinks / Output Devices
    # +++++++++++++++++++++++++++++++++++++++++++++++++++++


    # System (aka. Main)
    {   name = libpipewire-module-loopback
        args = {
            node.name = "virtual_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.main"
            node.description = "RODECaster Duo System"
            audio.position = [ FL FR ]

            capture.props = { media.class = "Audio/Sink" }

            playback.props = {
                target.object = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
                stream.dont-remix = true
                audio.position = [ AUX0 AUX1 ]
            }
        }
    },

    # Game
    {
        name = libpipewire-module-loopback
        args = {
            node.name = "virtual_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.game"
            node.description = "RODECaster Duo Game"
            audio.position = [ FL FR ]

            capture.props = { media.class = "Audio/Sink" }

            playback.props = {
                target.object = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
                stream.dont-remix = true
                audio.position = [ AUX2 AUX3 ]
            }
        }
    },

    # Music
    {
        name = libpipewire-module-loopback
        args = {
            node.name = "virtual_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.music"
            node.description = "RODECaster Duo Music"
            audio.position = [ FL FR ]

            capture.props = { media.class = "Audio/Sink" }

            playback.props = {
                target.object = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
                stream.dont-remix = true
                audio.position = [ AUX4 AUX5 ]
            }
        }
    },

    # Virtual A
    {
        name = libpipewire-module-loopback
        args = {
            node.name = "virtual_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.a"
            node.description = "RODECaster Duo Virtual A"
            audio.position = [ FL FR ]

            capture.props = { media.class = "Audio/Sink" }

            playback.props = {
                target.object = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
                stream.dont-remix = true
                audio.position = [ AUX6 AUX7 ]
            }
        }
    },

    # Virtual B
    {
        name = libpipewire-module-loopback
        args = {
            node.name = "virtual_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.b"
            node.description = "RODECaster Duo Virtual B"
            audio.position = [ FL FR ]

            capture.props = { media.class = "Audio/Sink" }

            playback.props = {
                target.object = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
                stream.dont-remix = true
                audio.position = [ AUX8 AUX9 ]
            }
        }
    }
]

# assign correct names to physical devices
node.rules = [
    # output
    {
        matches = [
            {
                node.name = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-1"
            }
        ]
        actions = {
            update-props = {
                node.description = "RODECaster Duo Multi-Channel"
                node.nick = "RODECaster Duo Multi-Channel"
            }
        }
    },
    {
        matches = [
            {
                node.name = "alsa_output.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-output-0"
            }
        ]
        actions = {
            update-props = {
                node.description = "RODECaster Duo Chat"
                node.nick = "RODECaster Duo Chat"
            }
        }
    },
    # input
    {
        matches = [
            {
                node.name = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-1"
            }
        ]
        actions = {
            update-props = {
                node.description = "RODECaster Duo Multi-Channel"
                node.nick = "RODECaster Duo Multi-Channel"
            }
        }
    },
    {
        matches = [
            {
                node.name = "alsa_input.usb-R__DE_RODECaster_Duo_{{DEVICE_SERIAL}}.pro-input-0"
            }
        ]
        actions = {
            update-props = {
                node.description = "RODECaster Duo Microphone"
                node.nick = "RODECaster Duo Microphone"
            }
        }
    },
]

# Force the pro-audio profile for the multichannel device
monitor.alsa.rules = [
  {
    matches = [
      {
        device.vendor.id = "0x19f7"
        device.product.id = "0x0095"
      }
    ]
    actions = {
      update-props = {
        device.profile = "pro-audio"
      }
    }
  }
]
